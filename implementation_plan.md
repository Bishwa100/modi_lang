# 🇮🇳 Modi Lang — The Deshbhakt Programming Language

> _"Mitron, aaj se coding bhi deshbhakti hai!"_

A hilarious, fully functional programming language where every keyword is a famous Modi catchphrase. Built as a tree-walking interpreter in **Node.js**.

---

## 🎯 Concept

Modi Lang is a **satirical esoteric programming language** that replaces boring keywords like `if`, `else`, `print`, and `function` with iconic Modi-isms. It's funny, it's functional, and it compiles your patriotism into actual programs.

---

## 🗣️ Language Keywords & Syntax

### Core Keywords Mapping

| Normal Keyword | Modi Lang Keyword | Why It's Funny |
|---|---|---|
| `let` / `var` | `modi` | _"Modi hai toh variable mumkin hai"_ — Modi creates everything |
| `print` | `mann_ki_baat` | _He speaks to the nation_ — now your program does too |
| `if` | `agar_modi` | _"Agar Modi chahein toh..."_ — conditional on Modi's will |
| `else` | `nahi_toh` | _"Nahi toh kya hoga?"_ — the alternative |
| `else if` | `warna_modi` | _Another condition, another promise_ |
| `true` | `acche_din` | _Good days = True. Always promised, sometimes delivered_ |
| `false` | `jumla` | _Jumla = False. Self-explanatory_ 😂 |
| `while` | `ghuma_ke` | _"Ghuma ke maarta hai"_ — loops around and around |
| `for` | `sabka_saath` | _"Sabka saath, sabka vikas"_ — iterate with everyone |
| `function` | `mitron` | _Every speech starts with "Mitron..."_ — every function too |
| `return` | `laut_aao` | _"Ghar wapsi"_ — return home with a value |
| `null` / `none` | `nota` | _NOTA = None of the Above_ — perfect for null |
| `input` | `chai_pe_charcha` | _"Chai pe charcha"_ — take input over chai_ |
| `import` | `make_in_india` | _Import? No! Make in India!_ |
| `try` | `modi_hai_toh` | _"Modi hai toh mumkin hai"_ — try it!_ |
| `catch` | `pakoda` | _When things go wrong, sell pakodas_ 🍳 |
| `break` | `demonetization` | _Breaks the loop just like it broke the economy_ 💸 |
| `continue` | `aage_badho` | _"Aage badho"_ — skip and move forward |
| `class` | `vishwaguru` | _World leader_ — the supreme class |
| `new` | `naya_bharat` | _"Naya Bharat"_ — create new instances |
| `delete` / `clear` | `swachh_bharat` | _Clean up!_ 🧹 |
| `end program` | `jai_hind` | _Every program ends with patriotism_ |
| `start program` | `namaste` | _Begin with Indian greeting_ 🙏 |
| `AND` | `aur` | _And in Hindi_ |
| `OR` | `ya` | _Or in Hindi_ |
| `NOT` | `bilkul_nahi` | _"Absolutely not!"_ |
| `==` | `same2same` | _Equality check, desi style_ |
| `!=` | `jumla_hai` | _"That's a jumla!"_ — not equal |
| `comment` | `// suno mitron` | _Listen friends..._ (ignored by interpreter) |

### Data Types

| Type | Modi Lang Name | Example |
|---|---|---|
| Number | `Aankda` | `modi votes = 303;` |
| String | `Bhaashan` (Speech) | `modi msg = "Mitron!";` |
| Boolean | `Acche_din / Jumla` | `modi sach = acche_din;` |
| Array | `Rally` | `modi crowd = [1, 2, 3];` |
| Null | `Nota` | `modi result = nota;` |

### Operators

| Operation | Modi Lang | Example |
|---|---|---|
| Addition | `jodo` or `+` | `a + b` or `a jodo b` |
| Subtraction | `ghata` or `-` | `a - b` or `a ghata b` |
| Multiply | `guna` or `*` | `a * b` |
| Divide | `baanto` or `/` | `a / b` |
| Modulo | `baaki` or `%` | `a % b` |
| Assignment | `=` | `modi x = 10;` |

---

## 📝 Example Programs

### 1. Hello India! 🇮🇳
```
namaste

modi greeting = "Mitron, Bhaiyo aur Behno!";
mann_ki_baat(greeting);
mann_ki_baat("Acche din aane wale hain!");

jai_hind
```
**Output:**
```
Mitron, Bhaiyo aur Behno!
Acche din aane wale hain!
```

### 2. Check Voting Eligibility
```
namaste

modi age = 19;

agar_modi (age >= 18) {
    mann_ki_baat("Aap vote de sakte hain! Desh ka future aapke haath mein!");
} nahi_toh {
    mann_ki_baat("Abhi padhai karo, voting baad mein!");
}

jai_hind
```

### 3. Count Modi's Rallies (Loop)
```
namaste

modi rally_count = 0;

ghuma_ke (rally_count < 5) {
    rally_count = rally_count + 1;
    mann_ki_baat("Rally number: " + rally_count);
}

mann_ki_baat("Total rallies: " + rally_count + " 🎉");

jai_hind
```

### 4. Functions — Mann Ki Baat Episode
```
namaste

mitron calculate_tax(income) {
    agar_modi (income > 500000) {
        laut_aao income * 0.3;
    } nahi_toh {
        laut_aao 0;
    }
}

modi salary = 800000;
modi tax = calculate_tax(salary);
mann_ki_baat("Aapka tax hai: ₹" + tax);
mann_ki_baat("Digital India mein sab transparent hai! 💻");

jai_hind
```

### 5. FizzBuzz — Modi Edition 🎪
```
namaste

// suno mitron: ye hai Modi style FizzBuzz

sabka_saath (modi i = 1; i <= 30; i = i + 1) {
    agar_modi (i % 15 same2same 0) {
        mann_ki_baat("Acche Din! 🎉");
    } warna_modi (i % 3 same2same 0) {
        mann_ki_baat("Modi! 🇮🇳");
    } warna_modi (i % 5 same2same 0) {
        mann_ki_baat("Mitron! 🎤");
    } nahi_toh {
        mann_ki_baat(i);
    }
}

jai_hind
```

---

## 😤 Error Messages (The Best Part!)

| Error Type | Modi Lang Error Message |
|---|---|
| Syntax Error | `"Mitron, ye kya likh diya? Galat syntax hai! 🤦"` |
| Undefined Variable | `"Ye variable toh Note-bandi mein chala gaya! 💸 '{name}' not found."` |
| Division by Zero | `"Zero se divide? Ye toh jumla hai mitron! ➗🚫"` |
| Type Error | `"Mitron, ye types match nahi kar rahe! Chai aur pakoda mix mat karo! 🍵🍳"` |
| Stack Overflow | `"Itna recursion? Modiji bhi thak jayenge! 😵‍💫"` |
| Missing Semicolon | `"Semicolon bhool gaye? Digital India mein precision zaroori hai! ⌨️"` |
| File Not Found | `"File nahi mili! Shayad Swachh Bharat mein saaf ho gayi! 🧹"` |
| Missing `jai_hind` | `"Program khatam karo 'jai_hind' ke saath! Deshbhakti dikhao! 🇮🇳"` |
| Missing `namaste` | `"Pehle namaste toh bolo! Tameez se shuru karo! 🙏"` |
| Unknown Token | `"Ye kya hai mitron? '{token}' toh hamare dictionary mein nahi hai!"` |
| Too Many Arguments | `"Itne arguments? Ye Mann Ki Baat hai, Parliament nahi! 🏛️"` |
| Runtime Crash | `"BREAKING: Program crashed! Blame it on Congress! 😂"` |

---

## 🏗️ Architecture — Interpreter Design

```
┌──────────────┐     ┌──────────┐     ┌──────────┐     ┌───────────┐
│  Source Code  │────▶│  Lexer   │────▶│  Parser  │────▶│ Evaluator │
│  (.modi)     │     │ (Tokens) │     │  (AST)   │     │ (Execute) │
└──────────────┘     └──────────┘     └──────────┘     └───────────┘
                                                             │
                                                             ▼
                                                       ┌───────────┐
                                                       │  Output   │
                                                       │ (Console) │
                                                       └───────────┘
```

### Components

1. **Lexer (Tokenizer)** — Reads `.modi` source files and converts them into tokens
   - Maps Modi keywords → token types
   - Handles strings, numbers, identifiers, operators
   - Tracks line/column for hilarious error messages

2. **Parser** — Converts token stream into an AST (Abstract Syntax Tree)
   - Handles expressions, statements, blocks
   - Validates `namaste` ... `jai_hind` program structure
   - Supports operator precedence

3. **Evaluator (Interpreter)** — Walks the AST and executes the program
   - Variable scoping (function-level)
   - Built-in functions (`mann_ki_baat`, `chai_pe_charcha`)
   - Funny runtime error handling

4. **REPL** — Interactive mode for quick experiments
   - Colorful terminal UI with Modi-themed prompt
   - `>>> Mitron, kya bolte ho? >`

---

## 📁 Project Structure

```
Modi Lang/
├── package.json              # Project config
├── README.md                 # Documentation with examples
├── bin/
│   └── modi.js               # CLI entry point
├── src/
│   ├── lexer.js              # Tokenizer
│   ├── parser.js             # Parser → AST
│   ├── evaluator.js          # AST interpreter
│   ├── tokens.js             # Token types & keyword map
│   ├── ast.js                # AST node definitions
│   ├── environment.js        # Variable scope/environment
│   ├── errors.js             # Funny error messages
│   ├── builtins.js           # Built-in functions
│   └── repl.js               # Interactive REPL
├── examples/
│   ├── hello.modi            # Hello India
│   ├── fizzbuzz.modi         # FizzBuzz Modi edition
│   ├── calculator.modi       # Basic calculator
│   ├── fibonacci.modi        # Fibonacci series
│   └── guess_game.modi       # Number guessing game
├── tests/
│   ├── lexer.test.js         # Lexer tests
│   ├── parser.test.js        # Parser tests
│   └── evaluator.test.js     # Evaluator tests
└── web/                      # Bonus: Online playground
    ├── index.html
    ├── style.css
    └── playground.js
```

---

## 🚀 Phased Build Plan

### Phase 1: Foundation (Lexer + Tokens) 
- Define all token types and keyword mappings
- Build the lexer to tokenize `.modi` files
- Handle strings, numbers, identifiers, operators
- Implement line/column tracking for error messages

### Phase 2: Parser (AST Generation)
- Define AST node types
- Parse expressions (arithmetic, comparison, logical)
- Parse statements (`modi`, `mann_ki_baat`, `agar_modi`, `ghuma_ke`, `sabka_saath`)
- Parse function declarations (`mitron`) and calls
- Validate `namaste`/`jai_hind` program boundaries

### Phase 3: Evaluator (Execution Engine)
- Implement environment/scope management
- Evaluate expressions and statements
- Implement built-in functions
- Wire up the hilarious error messages
- Support string concatenation, type coercion

### Phase 4: CLI + REPL
- Create the `modi` CLI command
- Build interactive REPL with colored output
- File execution mode (`modi run hello.modi`)
- Help command with examples

### Phase 5: Example Programs & Polish
- Write all example `.modi` programs
- Add comprehensive README with installation & usage
- Create fun ASCII art banner
- Terminal colors and emoji support

### Phase 6 (Bonus): Web Playground 🌐
- Browser-based Modi Lang editor
- Live execution with output panel
- Syntax highlighting
- Share code snippets

---

## ✅ Verification Plan

### Automated Tests
- Unit tests for lexer (tokenization of all keywords)
- Unit tests for parser (AST generation for all statement types)
- Integration tests running example `.modi` programs
- Error message tests (verify funny messages appear correctly)
- Run with: `npm test`

### Manual Verification
- Run each example program via CLI
- Test REPL interactive mode
- Verify error messages display correctly with emojis
- Test edge cases (missing `jai_hind`, undefined variables, etc.)

### Browser Testing (Phase 6)
- Test web playground in browser
- Verify code execution and output display

---

> [!IMPORTANT]
> **User Review Needed**: Please review the keyword mappings and humor. Should I add/remove/modify any keywords? Any other funny catchphrases you want included? Also, should I prioritize the web playground or keep it as a bonus phase?

> [!NOTE]
> The language will be implemented as a **tree-walking interpreter** in Node.js. This is the simplest approach that gives us a fully functional language. No compilation step needed — just `modi run yourfile.modi` and it runs!
